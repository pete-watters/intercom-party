import { customerDistance } from 'intercom-party/helpers/customer-distance';
import { module, test } from 'qunit';

module('Unit | Helper | customer distance');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = customerDistance([42]);
  assert.ok(result);
});
